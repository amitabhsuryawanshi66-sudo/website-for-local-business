/**
 * V5 Section Composer
 * -------------------
 * Determines which components to render based on niche playbook.
 */

export const composeSections = (playbook) => {
  return playbook.sectionFlow;
};
